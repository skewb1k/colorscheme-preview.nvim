# colorscheme-preview.nvim

A tiny Neovim plugin that lets you preview colorschemes live with `:colorscheme`.

## Install

```lua
vim.pack.add({ "https://github.com/skewb1k/colorscheme-preview.nvim" })
```
